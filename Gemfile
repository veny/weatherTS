source 'https://rubygems.org'

#ruby=ruby-2.4
#ruby-gemset=weatherTS

gem 'json', '2.0.2' # hack to avoid installation of native gem of latest version in the Alpine Docker image
gem 'influxdb'
gem 'chunky_png'
gem 'sinatra'
